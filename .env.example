#目前 docker-compose 配置是直接寫死，如果要用 .env 設定，請複製此檔案並命名為 .env ，然後再去改compose檔案中的環境變數格式


# 資料庫密碼 (可選，預設 household123)
MYSQL_PASSWORD=household123

# 排程設定
ENABLE_SCHEDULER=true
SCHEDULE_MODE=cron
SCHEDULE_HOUR=17
SCHEDULE_MINUTE=17

# ============================================================
# 異常通知設定
# ============================================================
# 是否啟用通知 (true/false)
NOTIFICATION_ENABLED=false

# SMTP 伺服器設定 (預設使用 Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# 寄件人帳號 (Gmail 需使用應用程式密碼)
# 取得 Gmail 應用程式密碼: https://myaccount.google.com/apppasswords
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password
